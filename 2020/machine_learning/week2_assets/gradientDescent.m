function gradient = gradientDescent(X, y, theta, alpha, numIters)
  
  % Gradient Descent performs gradient descent to learn theta
  
  % theta = gradientDescent(X, y, theta, alpha, numIters) updates theta by 
  
  % taking num_iters gradient step with learning rate alpha
    